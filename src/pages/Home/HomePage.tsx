import { type JSX } from 'react';

import SelectSetContainer from '@/containers/SelectSetContainer/SelectSetContainer';

const HomePage = (): JSX.Element => {
  return <SelectSetContainer />;
};

export default HomePage;
